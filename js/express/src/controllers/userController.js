const login = (req, res) => {
  res.send('Login Logik hier implementieren.');
};

const register = (req, res) => {
  res.send('Registrierungslogik hier implementieren.');
};

export const userController = {
  login,
  register,
};
